import { TestWorldsArrayFactory } from './test-worlds-array-factory';

describe('TestWorldsArrayFactory', () => {
  it('should create an instance', () => {
    expect(new TestWorldsArrayFactory()).toBeTruthy();
  });
});
